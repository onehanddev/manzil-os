import { PagePlaceholder } from '@/components/page-placeholder'

export function BillingPage() {
  return (
    <PagePlaceholder
      title="Billing"
      description="Charge types, maintenance rates, and the monthly charge-generation run"
    />
  )
}
